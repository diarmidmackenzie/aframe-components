{
  "name": "fusable-body",
  "version": "0.0.1",
  "description": "PhysX bodies that can be fused together",
  "main": "index.js",
  "scripts": {
    "dist": "npm run dist:dev && npm run dist:prod",
    "dist:dev": "webpack --config webpack.config.js",
    "dist:prod": "webpack --config webpack.prod.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "A-Frame",
    "physx"
  ],
  "author": "Diarmid Mackenzie",
  "license": "MIT",
  "dependencies": {
    "aframe-graph": "^0.0.1",
    "aframe-object-parent": "^0.1.0",
    "aframe-raycast-target": "^0.1.0"
  },
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
